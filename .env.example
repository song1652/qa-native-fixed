# test_data.json에서 ENV: 접두사가 있는 값에 대응하는 환경변수
TEST_SAINTCORE_PASSWORD=your_password_here

# DOM 캐시 TTL 설정 (선택, 기본값 사용 시 생략 가능)
# 정적 DOM 캐시 유효 시간 (시간 단위, 기본: 168 = 7일)
DOM_CACHE_TTL_HOURS=168
# 동적 요소(hover/클릭/우클릭 트리거) 캐시 유효 시간 (시간 단위, 기본: 24)
DOM_DYNAMIC_CACHE_TTL_HOURS=24
